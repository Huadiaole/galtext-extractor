# 默认：交给 Git 判断文本/二进制，仓库内统一 LF
* text=auto eol=lf

# Windows 批处理 / PowerShell 必须是 CRLF，否则 cmd.exe 可能解析异常
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# 二进制资源
*.png binary
*.ico binary
*.pdf binary
*.ttf binary
*.ttc binary
*.xp3 binary

# 不要给脚本加 working-tree-encoding：游戏脚本多为 Shift-JIS，
# 编码判定由 galtext.textkit 在运行时完成。
